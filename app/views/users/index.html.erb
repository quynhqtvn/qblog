<h1 id="page_title"> All Users </h1>

<div id="posts_wrapper" class="skinny_wrapper">
	<% @users = User.all %>
	<% @users.each do |user| %>
	  <p>ID: <%= user.id %><br>
	  Email: <%= link_to user.email, user_path(user)%></p>
	<% end %>
</div>
